function Products() {
    return (
        <div>
            <h1 className="font-bold">Take a look into our products! (We don't have yet)</h1>
            <p>Here would be the products list... If only I could consume the API</p>
            <br />
            <p className="text-red-500">Try writting any route in the URL</p>
        </div>
    );
}

export default Products